In $\mathbb{R}^k$ with $C \subseteq \mathbb{R}^k$, the following are equivalent:

(a) The set $C$ is a [[Compact Sets]].

(b) The set $C$ is both a [[Closed Sets]] and bounded.

The proof for this relies on the sequential characterization of compact sets.